<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/containerLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        xmlns:navigation="http://schemas.android.com/apk/res-auto"
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_gravity="start"
        android:layout_height="match_parent"
        android:background="@color/navigationBackgroundColor"
        navigation:headerLayout="@layout/navigation_header_view"
        navigation:itemIconTint="@color/navigation_items_tint"
        navigation:itemTextColor="@color/navigation_items_tint"
        navigation:menu="@menu/navigation" />

</android.support.v4.widget.DrawerLayout>